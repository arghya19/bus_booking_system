# bus_booking_system
A Bus Booking System built with Java (backend), Java Server Pages (JSP) for the frontend, and MySQL as the database, enabling users to search buses, book seats, manage reservations, and view booking history with secure authentication, validation, and admin management for efficient online ticketing.
